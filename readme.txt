executar o servidor com:
python -m server.server

executar cada usuário com:
python -m client.client

usuarios: alice (senha: 1234)
        bob (senha abcd)


Deixar 3 terminais rodando simultaneamente, um servidor (primeiro) e dois terminais com usuários (após execução do servidor)